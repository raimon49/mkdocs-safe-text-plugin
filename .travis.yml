language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7-dev"
install:
  - pip install -r dev-requirements.txt
script:
  py.test --pep8 -v --cov mkdocssafetext
after_success:
  - if [[ $TRAVIS_PYTHON_VERSION == 2* ]]; then codecov; fi
